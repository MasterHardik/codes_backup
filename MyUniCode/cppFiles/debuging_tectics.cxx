//Write a program to make a matrix multiplication

#include <iostream>
using namespace std;
#define debug(x) cerr << #x << " " << x << endl;
int main()
{
	int a = 1;
	int b = 2;
	char c = 65;
	char ch = 40;
	debug(a);debug(b);debug(c);debug(ch);
	return 0;
}