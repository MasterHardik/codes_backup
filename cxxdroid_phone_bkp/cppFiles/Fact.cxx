//#include<stdio.h>


//The program can even run without the use of pre processor directive also ..it is only required whenever we add on extras which are defined in the library itself only ..
int main(){
	return 1;
	
	}